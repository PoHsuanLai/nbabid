type Users {
    id: ID!
    username: String!
    password: String!
    cash: Int!
    bid: String
}

type Bid {
    id: ID!
    gameID: ID!
    bidFor: String!
    bidMoney: Int!
    result: Int
}

type Mutation {
    createUser(input: signUpInput!): Users!
    updateCash(input: CashInput!): Users!
    createBid(name: String!, bid: String!, price: Int!): Users!
}

type Query {
    users(input: userQuery!): Users
    games: [Games!]
    bid(input: bidQueryMany!): [Bid]
}

type Games {
    id: ID
    date: String
    top: String
    topScore: Int
    bot: String
    botScore: Int
}

type Subscription {
    userCreated: Users!
    cashUpdated: ID!
    bidCreated: Users!
}


input signUpInput {
    id: ID!
    username: String!
    password: String!
    cash: Int!
    bid: String!
}

input userQuery {
    username: String!
    password: String!
}

input bidQueryMany {
    username: String!
}